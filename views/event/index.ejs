<div class="sch-b-content--default">
  <h1>All my events</h1>
  <% for (event of events) {%>
    Event name: <a href="/event/<%= event.id%>"><%= event.name %></a><br>
    Start date: <%= event.startDate %><br>
    End date: <%= event.endDate%>
    <br><br>
  <%}%>
 </div>
